<Window x:Class="BarCodeSystem.ProductDispatch.FlowCard.DisPlanConfirm_Window"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Loaded="Window_Loaded"
        ResizeMode="NoResize"
        Title="派工方案确认界面" Height="350" Width="350" Icon="/BarCodeSystem;component/Images/burn.ico" >
    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition></RowDefinition>
            <RowDefinition Height="30"></RowDefinition>
        </Grid.RowDefinitions>
        <Grid.ColumnDefinitions>
            <ColumnDefinition></ColumnDefinition>
            <ColumnDefinition></ColumnDefinition>
        </Grid.ColumnDefinitions>
        <Frame Name="frame_Confirm" ContentRendered="frame_Confirm_ContentRendered"   Padding="5" Margin="5" Grid.ColumnSpan="2"></Frame>
        
        <Button Grid.Row="1" Style="{StaticResource btn_CenterStyle}" Content=" 完      成  " Name="btn_Finish" Click="btn_Finish_Click"></Button>
        <Button  Grid.Column="1" Grid.Row="1" Style="{StaticResource btn_CenterStyle}" Content="  取     消  " Name="btn_Cancel" Click="btn_Cancel_Click"></Button>
    </Grid>
</Window>
